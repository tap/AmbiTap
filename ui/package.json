{
  "name": "@ambitap/ui",
  "version": "0.1.0",
  "private": true,
  "description": "AmbiTap UI widgets: one framework-free model, pluggable renderers (HTML5 canvas, Max v8ui mgraphics). See UI.md.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "tsc -p tsconfig.json && node --test \"build/test/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  }
}
